{
  "name": "__APP_NAME_KEBAB__",
  "private": true,
  "type": "module",
  "description": "WiXAnything companion app: ships a code-owned Site Widget (auto-placed on the homepage) plus an Editor Add-on panel to configure it inside Wix Studio.",
  "scripts": {
    "dev": "wix dev",
    "build": "wix app build",
    "preview": "wix preview"
  },
  "dependencies": {
    "@wix/editor": "^1.566.0",
    "@wix/sdk": "^1"
  },
  "devDependencies": {
    "@wix/cli": "^1"
  }
}
