# Wix Headless OAuth client id (public, safe to ship to the browser).
# Get it from: Wix dashboard → Settings → Headless → create an OAuth app (Client / "Visitor").
# Point that Headless project at the SAME site whose data you want to reuse.
WIX_CLIENT_ID=
