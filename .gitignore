# ── secrets / env (NEVER commit real env files) ──────────────────────────────
.env
.env.*
!.env.example
*.pem
*.key

# ── private / local-only (kept on disk, never pushed) ────────────────────────
PARTNERSHIP-PITCH.md
docs/media/
# CI workflow is authored but the gh OAuth token lacks the 'workflow' scope to push it.
# To enable CI: `gh auth refresh -s workflow` then `git add -f .github/workflows/ci.yml`.
.github/workflows/ci.yml

# ── deps / build ─────────────────────────────────────────────────────────────
node_modules/
*.log
.DS_Store

# ── visual scraper (browser deps + generated output) ─────────────────────────
visual/node_modules/
visual/wix-visual.json

# ── local-only generated maps (don't publish a real siteId / element names) ──
examples/ottop/
/wix-elements.json
/wix-elements.md
